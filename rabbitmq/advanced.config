%% RabbitMQ Management path prefix for nginx proxy at /amqp/
%% When proxied, RabbitMQ serves UI and API under /amqp/ so OAuth redirect URIs are correct.
[
  {rabbitmq_management, [
    {path_prefix, <<"/amqp">>}
  ]}
].
